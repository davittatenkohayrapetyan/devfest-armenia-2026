{
  "name": "devfest-armenia-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "npm run build:board",
    "build": "tsc --noEmit && vite build",
    "prebuild": "npm run build:board",
    "build:board": "node scripts/build-board.mjs",
    "build:deploy": "node scripts/build-deploy.mjs",
    "preview": "vite preview",
    "validate:content": "node scripts/validate-content.mjs",
    "check:brand": "node scripts/check-brand.mjs"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
